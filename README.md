# mcp-figma-sandbox


Repo base con HTML estático para testear el MCP de Figma en local y validar organización del archivo de diseño.

## Estructura
- `index.html`: página base
- `assets/styles.css`: estilos (opcional)
- `assets/main.js`: JS (opcional)

## Uso
Abre `index.html` en el navegador o sirve con un http server local.
